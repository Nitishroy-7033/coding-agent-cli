# CLI Coding Agent

An agentic CLI coding tool powered by custom OpenAI-compatible endpoints.

## Installation

```bash
pip install -e .
```

## Quick Start

```bash
# Run CLI REPL
agent
```




now when we do any / command then my option should come as dropdown not only suggestion i can navigate and choose like when i type /mode the all three mode come as option i can choose anyone some for other option also, 